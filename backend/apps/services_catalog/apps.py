from django.apps import AppConfig


class ServicesCatalogConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.services_catalog"
    verbose_name = "Catálogo de serviços"
